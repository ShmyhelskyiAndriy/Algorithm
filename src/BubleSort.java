import java.util.Arrays;public class BubleSort extends Util {    static void bubleSort(int[] array){        boolean needIter = true;        while(needIter){            needIter = false;            for(int i = 1; i < array.length; i++){                if(array[i] < array[i-1]){                    swap(array, i, i-1);                    needIter = true;                }            }        }        //System.out.println(Arrays.toString(array));    }    static void bubleSortNeg(int[] array){        boolean needIter = true;        while(needIter){            needIter = false;            for(int i = 1; i < array.length; i++){                if(array[i] > array[i-1]){                    swap(array, i, i-1);                    needIter = true;                }            }        }        System.out.println(Arrays.toString(array));    }    public static void bubleSortLimit(int[] array, int start,int stop){        boolean needIter = true;        while(needIter){            needIter = false;            for(int i = start;  i <= stop ; i++){                if(array[i] < array[i-1]){                    swap(array, i, i-1);                    needIter = true;                }            }        }        //System.out.println(Arrays.toString(array));    }}