import java.lang.reflect.Array;import java.util.Random;public class Util {    public static void swap(int[] array, int ind1, int ind2){        int tmp=array[ind1];        array[ind1]=array[ind2];        array[ind2]=tmp;    }    static int partition (int arr[], int first, int last) {        int pivot = arr[last];        int i = (first - 1);        for (int j = first; j < last; j++) {            if (arr[j] <= pivot) {                i++;                int temp = arr[i];                arr[i] = arr[j];                arr[j] = temp;            }        }        int temp = arr[i + 1];        arr[i + 1] = arr[last];        arr[last] = temp;        return i + 1;    }    static void filling(int arr[]){        Random random = new Random();        for (int i = 0; i < arr.length; i++) {        int randNum = random.nextInt(100) + 1;        arr[i] = randNum;        }    }    static int randOp(int startLim, int stopLim){        Random random = new Random();        int randNum = random.nextInt(stopLim-startLim) + 1 + startLim;        return randNum;    }    static void merge(int arr[], int arrLeft[], int arrRight[]){        int i = 0;        int j = 0;        int id = 0;        while (i < arrLeft.length && j < arrRight.length) {            if (arrLeft[i] < arrRight[j]) {                arr[id] = arrLeft[i];                i++;            } else {                arr[id] = arrRight[j];                j++;            }            id++;        }        while (i < arrLeft.length) {            arr[id] = arrLeft[i];            i++;            id++;        }        while (j < arrRight.length) {            arr[id] = arrRight[j];            j++;            id++;        }    }        static void mergeDegrease(int arr[], int arrLeft[], int arrRight[]){            int i = 0, j = 0, idx = 0;            while (i <  arrLeft.length && j < arrRight.length) {                if ( arrLeft[i] >=arrRight[j]) {                    arr[idx] = arrLeft[i];                    i++;                } else {                    arr[idx] = arrRight[j];                    j++;                }                idx++;            }            while (i < arrLeft.length) {                arr[idx] =  arrLeft[i];                i++;                idx++;            }            while (j <arrRight.length) {                arr[idx] = arrRight[j];                j++;                idx++;            }        }        static int maxFrom(int arr[]){            int max = 0;            for (int i = 0; i < arr.length; ++i){                if(arr[i]>=max){                    max = arr[i];                }            }            return max;        }}